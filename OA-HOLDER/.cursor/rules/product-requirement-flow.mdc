---
description: 当用户以「一句话需求/按默认协议/按 PM 流程」发起需求时，按需求工厂完整流程执行（P0 澄清 → S01-S19 → 可选自动化验收）
alwaysApply: false
---

# 一句话需求 → 需求工厂完整流程

## 何时触发

当用户消息满足以下**任一**情况时，本规则生效，你必须按「需求工厂」执行：

- 消息以 **`一句话需求:`** 或 **`一句话需求：`** 开头（中英文冒号均可）
- 消息包含 **`按默认协议`** 或 **`按 product-manager 流程`** 或 **`按 PM 流程`**
- 用户明确说 **「帮我分析需求」**、**「走需求流程」**、**「出需求卡和原型」**
- 用户通过 **`/pm-flow`** 显式调用（对应 `.cursor/commands/pm-flow.md`）

## 不触发时的处理

- 若用户未触发上述条件，按常规对话处理，不强制进入本流程。
- 若用户仅咨询概念/格式/工具用法（非需求立项），不得误进入 P0 澄清流程。

## 你必须执行的动作

1. **先读技能**：读取并遵循 `.cursor/skills/requirement-factory/SKILL.md` 中的流程与步骤。
2. **第一轮回复**：只输出 **P0 澄清问题清单**（用户与场景、对象与范围、主路径、规则与边界、验收口径），不要跳过直接给方案或代码。
3. **等用户回答**：用户回复澄清答案后，再进入 S01-S19 分析与方案阶段；若用户已确认方案，再执行文档与可交互原型交付。
4. **原型须按功能清单实现**：
   - **唯一依据**：原型的实现范围**仅**以同模块的 `03-功能清单与验收标准-xxx.md` 中的功能清单表格为准，逐条实现或显式标注「本期不做/占位」。
   - **强制交付物**：原型交付时，**必须**在同模块 03 文档中产出或更新**「原型实现情况对照」**表。
5. **功能清单防遗漏**：产出 03-功能清单时，必须按 SKILL 中的「功能清单生成与自检」八步执行，并**产出自检表**；03 定稿前，必须调用 product-expert 做**功能清单专项审查**。
6. **自动化验收（可选）**：原型生成后，在用户确认「执行自动化验收」后，生成验收脚本、运行验收、产出验收问题表。

## 产物输出路径

- **默认**：`docs/pro_product_Engineer/required-product/{需求关键词}_{YYYY-MM-DD}/`
- **可选 pm-flow 风格**：`原型/模块/<模块>/` 下产出 01-需求卡、02-需求方案、03-功能清单

## 禁止行为

- 用户发了「一句话需求: xxx」时，**不得**直接给实现方案或写代码，必须先问 P0 澄清问题。
- 不得忽略「先问清楚再写」的确认机制。

## 用户可复制的一句话模板

```
一句话需求: [这里写需求描述]
```

可选补充：`可选补充: 现有页面 @xxx，目标用户 xxx，约束 xxx`
