---
description: Git Commit 消息规范 - 必须使用中文描述
alwaysApply: true
---

# Git Commit 消息规范

生成 Git commit 消息时，**必须使用中文**，遵循以下格式：

```
<类型>: <简短描述>

[可选的详细说明]
```

## 类型说明

| 类型 | 用途 |
|------|------|
| `feat` | 增加新功能 |
| `fix` | 修复问题/BUG |
| `style` | 代码风格调整（不影响运行结果） |
| `perf` | 性能优化 |
| `refactor` | 代码重构 |
| `revert` | 撤销修改 |
| `test` | 测试相关 |
| `docs` | 文档/注释 |
| `chore` | 依赖更新/脚手架配置修改 |
| `workflow` | 工作流改进 |
| `ci` | 持续集成 |

## 示例

```
feat: 实现主窗口托盘图标管理功能
```

```
fix: 修复窗口关闭后托盘菜单未更新的问题

点击关闭按钮时，托盘菜单的显示状态未同步更新
```

```
refactor: 重构 IPC 通信处理模块

将分散的 ipcMain 监听拆分为独立的 handler 文件，提升可维护性
```

## 重要提示

- 类型标识使用英文（`feat`、`fix` 等），**描述必须是中文**
- 保持描述简洁明了，不超过 50 个字
- 需要补充说明时，空一行后添加详细内容（同样使用中文）
