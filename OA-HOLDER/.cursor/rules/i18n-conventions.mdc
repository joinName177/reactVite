---
description: 国际化规范：locale 文件结构、key 命名（camelCase）、禁止硬编码文案、chooseLanguage 用法、表单校验、API 消息、getNameTmpl
globs: **/*.{ts,tsx}
alwaysApply: false
---

# 国际化（i18n）规范

## 核心原则

UI 中**禁止硬编码中文或英文文案**，所有用户可见文字必须通过 locale 文件或 `chooseLanguage` 管理。

## 支持语言

- `LanguageType.ZhCn`：简体中文
- `LanguageType.EnGb`：英文（en-GB）

## Locale 文件结构

每个模块在同级目录创建 `locale.ts`，key 使用 **camelCase**，值提供所有支持语言：

```typescript
// modules/user/locale.ts
import { LanguageType } from '@holder/i18n'

type L = Record<LanguageType, string>

export const userLocale = {
  // ==================== 页面标题 ====================
  pageTitle: {
    [LanguageType.ZhCn]: '用户管理',
    [LanguageType.EnGb]: 'User Management',
  } as L,

  // ==================== 表单 ====================
  nameLabel: {
    [LanguageType.ZhCn]: '姓名',
    [LanguageType.EnGb]: 'Name',
  } as L,
}
```

## Key 命名规范

- 使用 **camelCase**（小驼峰）
- 语义化，描述用途而非内容
- 按功能分组并添加注释分隔

```
✅ accountPlaceholder、loginBtn、confirmMismatch
❌ account_placeholder、LoginBtn、confirm_mismatch_error
```

## 组件中使用方式

### 方式一：通过 locale + t 封装（推荐）

```typescript
import { useI18n } from '@holder/i18n'
import { userLocale } from './locale'

const MyComponent = () => {
  const { chooseLanguage } = useI18n()

  const t = useCallback(
    (key: keyof typeof userLocale, param?: Record<string, unknown>) =>
      chooseLanguage({ tmpl: userLocale[key], param }),
    [chooseLanguage],
  )

  return <button>{t('loginBtn')}</button>
}
```

### 方式二：内联 chooseLanguage（少量文案时）

```typescript
import { useI18n, LanguageType } from '@holder/i18n'

const MyComponent = () => {
  const { chooseLanguage } = useI18n()

  return (
    <span>
      {chooseLanguage({
        tmpl: {
          [LanguageType.ZhCn]: '中文文案',
          [LanguageType.EnGb]: 'English text',
        },
      })}
    </span>
  )
}
```

## 带变量的文案

模板中使用 `{变量名}` 占位符：

```typescript
// locale.ts
passwordMinLength: {
  [LanguageType.ZhCn]: '密码长度至少{min}个字符',
  [LanguageType.EnGb]: 'Password must be at least {min} characters',
} as L,

// 使用时
t('passwordMinLength', { min: 8 })

// 或内联
chooseLanguage({
  tmpl: {
    [LanguageType.ZhCn]: '共有 {count} 人',
    [LanguageType.EnGb]: 'Total {count} people',
  },
  param: { count: 5 },
})
```

## 表单校验消息

在 `Form.Item` 的 `rules[].message` 中使用 `chooseLanguage` 或 `t`：

```typescript
// 通过 t 函数（推荐）
<Form.Item
  name="username"
  rules={[
    {
      required: true,
      message: t('accountRequired'),
    },
  ]}
/>

// 或内联 chooseLanguage
<Form.Item
  name="username"
  rules={[{
    required: true,
    message: chooseLanguage({
      tmpl: {
        [LanguageType.ZhCn]: '请输入用户名',
        [LanguageType.EnGb]: 'Please enter username',
      },
    }),
  }]}
/>
```

## API 消息（非 React 场景）

API 拦截器、错误处理等非 React 代码中，**禁止硬编码错误文案**。推荐：

1. 在 `locale.ts` 或独立的 `messages.ts` 中定义 API 相关文案
2. 通过 `localStorage` 或全局状态获取当前语言，再调用 `chooseLanguage` 或等价逻辑

若项目建立 `messages.ts`，可提供 `getApiMessage`、`getApiMessageWithParam` 等工具，供非 React 代码使用。新增 API 消息时，需同时提供各语言 key。

## getNameTmpl / getLocalizedName

当后端数据为 `{ en, zh }` 结构时，使用以下工具：

```typescript
import { getNameTmpl, getLocalizedName, useI18n } from '@holder/i18n'

// 将 { en, zh } 转为 chooseLanguage 所需的 tmpl 格式
const tmpl = getNameTmpl({ en: 'Hello', zh: '你好' })
// { [LanguageType.ZhCn]: '你好', [LanguageType.EnGb]: 'Hello' }

// 根据当前语言直接取文案（非 React 或已有 language 时）
const { language } = useI18n()
const name = getLocalizedName({ en: 'Hello', zh: '你好' }, language)
// language === ZhCn 时 => '你好'
```
